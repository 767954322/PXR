<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="listener"
            type="com.diting.pingxingren.listener.ClickListener"/>
    </data>

    <LinearLayout
        style="@style/CommonLayout">

        <android.support.v7.widget.RecyclerView
            android:id="@+id/commonRecycler"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="@dimen/dimen_10_dp"
            android:layout_weight="0.15"/>

        <RelativeLayout
            android:id="@+id/tv_add_common_language"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="0.9">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:layout_centerHorizontal="true"
                android:layout_centerVertical="true"
                android:drawableLeft="@drawable/ic_add_knowledge_p"
                android:drawablePadding="@dimen/dimen_5_dp"
                android:drawableStart="@drawable/ic_add_knowledge_p"
                android:gravity="center"
                android:text="@string/add_to_the_knowledge_base"
                android:textColor="@color/commonColor"
                android:textSize="@dimen/text_size_16" />
        </RelativeLayout>

    </LinearLayout>
</layout>